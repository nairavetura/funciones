function calcularPerimetro(longitud) {
    return longitud * 4
}

const longitud = 8;
const perimetro = calcularPerimetro(longitud)
console.log(`El perimetro del cuadrado con lado de longitud ${longitud} es: ${perimetro}`);